package com.pathfinder;

/**
 * Interface for FilterableItemAdapter which specifies an object's value by which is should be filtered
 * Connor Reeder
 */

public interface FilterableItem {
    public String valueToFiler();
}
